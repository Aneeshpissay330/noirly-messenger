import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  photoCard: {
    borderRadius: 12,
    overflow: 'hidden',
  },
  photoCover: {
    backgroundColor: 'transparent',
  },
});
